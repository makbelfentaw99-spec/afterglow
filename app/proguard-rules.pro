# Add project specific ProGuard rules here.
# Minification is off for now (see app/build.gradle.kts); flip isMinifyEnabled
# to true once you're closer to a release build and add rules here as needed.
